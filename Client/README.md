# RPS Client

React + Vite admin UI for the Resource Planning System.

## Setup
- Copy `.env.example` to `.env` and update values if needed.
- Install dependencies: `npm install`
- Start dev server: `npm run dev`

The client expects the API at `VITE_API_URL` + `VITE_API_BASE`.
